#pragma once

// Include SDKDDKVer.h to define the highest version of the Windows platform available
// If you are generating an application for a previous Windows platform, include WinSDKVer.h,
// set the _WIN32_WINNT macro to the platform you want to support, and then include SDKDDKVer.h

#include <SDKDDKVer.h>
